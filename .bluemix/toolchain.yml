---
default_locale: en
locales: 
  $ref: locales.yml
name: "Build your own toolchain"
description: "This toolchain has no preconfigured tools. If you are already familiar with toolchains, you can set up your own toolchain.\n\nTo get started, click **Create**."
version: 0.1
this:
 - is an
 - array
my-great-service:
service_id: fizbzuzz
  parameters:
   repo_name: "micro1-{{name}}"
   repo_url: "https://whatever"
   type: "clone"
has_issues: false